# MkDocs Requirements for Spruce Bot Documentation
# Optimized for minimal dependencies based on actual usage

# Core MkDocs
mkdocs>=1.5.0,<2.0.0
mkdocs-material>=9.4.0,<10.0.0

# Essential Plugins (only those actually used in mkdocs.yml)
mkdocs-minify-plugin>=0.7.0

# Extensions (used for markdown features)
pymdown-extensions>=10.3.0
